@model IEnumerable<pg_web.Models.Device>

@{
    ViewBag.Title = "DevicesList";
}

<h2>DevicesList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.deviceType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deviceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deviceData)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.deviceType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deviceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deviceData)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.deviceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.deviceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.deviceId })
        </td>
    </tr>
}

</table>
